import React from "react";
import ReactDOM from "react-dom";
import { Form, Input, Button, Select } from "antd";

const RenderSurveyRequest = ({}) => {
  return <Form></Form>;
};

export default RenderSurveyRequest;
